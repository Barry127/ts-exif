import { ExifData, ExifOptions } from '../types';
import { ParsedMakerNote } from './types';

export function parseMakerNote(
  buffer: Buffer,
  options: ExifOptions,
  exif: ExifData
): ParsedMakerNote {
  return {};
}
